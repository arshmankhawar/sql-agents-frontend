# Backend API base URL.
# Leave empty in development — Vite's proxy (vite.config.ts) forwards /api to localhost:8000.
# Set to your deployed backend URL in production, e.g. https://api.yourdomain.com
VITE_API_URL=
